{
  "buildCommand": "npm run build",
  "outputDirectory": "ui/ui",
  "builds": [
    {
      "src": "api/**/*.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api/$1"
    },
    {
      "src": "/(.*)",
      "dest": "/ui/ui/$1"
    }
  ]
}
