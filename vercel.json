{
  "buildCommand": "npm run build",
  "outputDirectory": "ui/ui",
  "builds": [
    {
      "src": "api/v1/route.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/api/v1/(.*)",
      "dest": "/api/v1/route.ts"
    },
    {
      "src": "/(.*)",
      "dest": "/ui/ui/$1"
    }
  ]
}
